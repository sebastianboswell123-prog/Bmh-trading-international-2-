# Sanity (public, publishable values — safe to expose)
# These are optional: the app falls back to these same defaults in src/sanity/client.js
VITE_SANITY_PROJECT_ID=k5kbfta8
VITE_SANITY_DATASET=production
